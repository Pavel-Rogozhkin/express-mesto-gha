const createUser = (req, res) => {
  res.send(req.body);
};

const getUser = (req, res) => {
  res.send(req.body);
};

const getUserById = (req, res) => {
  res.send(req.body);
};

module.exports = {
  createUser,
  getUser,
  getUserById,
};
